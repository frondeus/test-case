###
# Ensure that `panics` syntax anh `#[should_panic]` macro works.
###

[package]
name = "cases_can_panic"
version = "0.1.0"
edition = "2021"

[dependencies]
[lib]
name = "cases_can_panic"
path = "src/lib.rs"
doctest = false

[dev-dependencies]
test-case = { path = "../../../" }

[workspace]
