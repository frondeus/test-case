###
# Ensure that test cases automatically assert return values.
###

[package]
name = "cases_can_return_result"
version = "0.1.0"
edition = "2024"

[lib]
name = "cases_can_return_result"
path = "src/lib.rs"
doctest = false

[dependencies]
test-case = { path = "../../../" }

[workspace]
