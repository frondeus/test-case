###
# Ensure that `with` syntax works.
###

[package]
name = "cases_support_keyword_with"
version = "0.1.0"
edition = "2021"

[lib]
name = "cases_support_keyword_with"
path = "src/lib.rs"
doctest = false

[dev-dependencies]
test-case = { path = "../../../" }

[workspace]
