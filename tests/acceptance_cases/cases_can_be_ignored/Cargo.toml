###
# Ensure that `=> ignore` syntax works.
###

[package]
name = "cases_can_be_ignored"
version = "0.1.0"
edition = "2021"

[lib]
name = "cases_can_be_ignored"
path = "src/lib.rs"
doctest = false

[dev-dependencies]
test-case = { path = "../../../" }

[workspace]
