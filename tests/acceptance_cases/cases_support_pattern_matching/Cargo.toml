###
# Ensure that `matches` syntax works.
###

[package]
name = "cases_support_pattern_matching"
version = "0.1.0"
edition = "2021"

[lib]
name = "cases_support_pattern_matching"
path = "src/lib.rs"
doctest = false

[dev-dependencies]
test-case = { path = "../../../" }

[workspace]
